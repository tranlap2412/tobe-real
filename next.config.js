/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    images: {
        allowFutureImage: true
    }
  }
}

module.exports = nextConfig

// next.config.js
module.exports = {
    reactStrictMode: true,
  };